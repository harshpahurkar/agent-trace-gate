# local state written by the agent hooks
.agent-trace/

# python
.venv/
__pycache__/
*.egg-info/
dist/
build/
.pytest_cache/

# node
node_modules/

# local env + exported artifacts
.env
traces.json

# graphify knowledge graph — regenerated from source in ~20s by
# `graphify update .` and the post-commit hook, so it is build output
graphify-out/
