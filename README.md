# agent-trace-gate

Runtime checkpoints for AI-generated code. Track what Claude Code and Cursor
write, execute it in an instrumented sandbox, and catch hallucinated imports,
fake APIs, and schema drift with OpenTelemetry traces — before it merges.

Work in progress. Quickstart and docs landing soon.
